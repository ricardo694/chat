services:
  - type: web
    name: ventoo-chatbot
    env: python
    region: oregon
    plan: free
    runtime: python3
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GROQ_API_KEY
        sync: false
